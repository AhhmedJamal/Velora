---
scope: Velora Laravel API (orders, products, customers, payments)
purpose: build-substrate (audience TBD)
altitude: feature
---

- (event) run started; coaching path chosen; memlog maintained by hand (no python/uv on host)
- (constraint) brownfield: Laravel ^13.17, PHP ^8.3, Sanctum 4, Pest 5, MySQL, database queue/cache/session
- (constraint) existing layering: Controller -> Service -> Repository; business rules in app/Domain/*/…Rules; enums in Domain/Orders and app/Enums
- (question) orders table still single-product (product_id, quantity) while OrderService writes order_items — which is canonical?
- (question) relation naming drift: items() used vs orderItems() defined
- (question) payment charged inside DB transaction before order persisted — ordering/atomicity with external gateway
- (question) AccountRole enum exists but order routes are unauthenticated; Customer is separate from User
- (question) product identity: single-merchant store vs marketplace vs logistics back office (.env DB name 'logiflow')
